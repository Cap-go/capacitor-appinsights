import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ee.forgr.capacitor_appinsights.example_app',
  appName: 'example-app',
  webDir: 'dist'
};

export default config;
